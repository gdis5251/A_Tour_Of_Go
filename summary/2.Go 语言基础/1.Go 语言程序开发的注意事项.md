# GO 程序开发的注意事项

1. Go 源文件是以 "go" 为扩展名。
2. Go 应用程序执行入口是 main 函数。
3. Go 语言严格区分大小写。
4. Go 方法由一条条语句构成，每条语句后不需要分号(Go 语言会在每行后自动加分号)。
5. Go 编译器是一行一行进行编译的，因此一行就只能写一条语句。若把多条语句写在同一行，会报错。
6. Go 编译器是一行一行进行编译的，因此一行就只能写一条语句。若把多条语句写在同一行，会报错。
7. Go 语言定义的变量或者 import 的包如果没有使用到，代码不能通过编译。
8. 大括号都是成对出现的，缺一不可。